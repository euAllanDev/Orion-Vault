"use client";
import Link from "next/link";
import { signIn } from "next-auth/react";
import { FormEvent, useState } from "react";
export default function Login() { const [email, setEmail] = useState(""); const [sent, setSent] = useState(false); const [error, setError] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setError(false); const result = await signIn("email", { email, redirect: false, callbackUrl: "/w/fieldnote-demo/dashboard" }); if (result?.ok) setSent(true); else setError(true); } return <main className="auth-page"><Link className="wordmark" href="/">Fieldnote</Link><section className="auth-card" aria-labelledby="login-title"><p className="eyebrow">ACESSO SEGURO</p><h1 id="login-title" tabIndex={-1}>Entre no Fieldnote</h1><p className="muted">Enviaremos um link de acesso válido por 15 minutos.</p><form onSubmit={submit}><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="owner@benchmark.test" /></div><button className="button" type="submit" disabled={sent}>{sent ? "Link enviado" : "Enviar link de acesso"}</button></form>{sent && <p className="notice" role="status">Verifique seu email para continuar.</p>}{error && <p className="notice" role="alert">Não foi possível enviar link. Tente novamente.</p>}<p className="helper">Ambiente local de benchmark: use identidade sintética @benchmark.test.</p></section></main>; }
