"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
export default function Login() { const [email, setEmail] = useState(""); const [sent, setSent] = useState(false); function submit(event: FormEvent) { event.preventDefault(); setSent(true); } return <main className="auth-page"><Link className="wordmark" href="/">Fieldnote</Link><section className="auth-card" aria-labelledby="login-title"><p className="eyebrow">ACESSO SEGURO</p><h1 id="login-title" tabIndex={-1}>Entre no Fieldnote</h1><p className="muted">Enviaremos um link de acesso válido por 15 minutos.</p><form onSubmit={submit}><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="voce@empresa.com" /></div><button className="button" type="submit" disabled={sent}>{sent ? "Link enviado" : "Enviar link de acesso"}</button></form>{sent && <p className="notice" role="status">Verifique {email.replace(/(.{2}).+(@.*)/, "$1•••$2")} para continuar.</p>}<p className="helper">Esta interface demonstra fluxo. Nenhum email é enviado sem provedor configurado.</p></section></main>; }
