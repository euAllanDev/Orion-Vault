# Desktop shell scaffold

This folder defines the future Electron shell for Marika.

The current goal is to keep the runtime local-first, reuse the existing TypeScript core, and wire the desktop window around the current vault workflows.
